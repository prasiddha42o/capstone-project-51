# Single requirements file for the whole project: the dataset pipeline,
# model-training notebooks, and the FastAPI backend all install from this
# file (see backend/Dockerfile, which builds from the repo root).

# --- Web framework (backend) ---
fastapi>=0.136.3
uvicorn[standard]>=0.22.0
python-multipart>=0.0.6
pydantic>=2.0.0

# --- Database (PostgreSQL / Supabase) ---
psycopg2-binary>=2.9.9
python-dotenv>=1.0.0

# --- Auth ---
PyJWT>=2.8.0
bcrypt>=4.1.0

# --- HTTP client (Supabase Storage uploads) ---
httpx>=0.28.0

# --- Data pipeline (waste_data_pipeline.py) ---
imagehash>=4.3.1
scikit-learn>=1.3.0
tqdm>=4.66.0

# --- Model training / inference (backend + notebooks) ---
torch>=2.0.0
torchvision>=0.15.0
Pillow>=10.0.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0

# --- Notebook environment ---
jupyter>=1.0.0
ipywidgets>=8.0.0

# --- Testing ---
pytest>=7.4.0
pytest-asyncio>=0.23.0