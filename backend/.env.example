SUPABASE_URL=
SUPABASE_SECRET_KEY=
JWT_SECRET_KEY=
DATABASE_URL=postgresql://postgres:password@localhost:5432/waste_assistant_nepal

# Optional overrides (sensible defaults are used if omitted):
# FRONTEND_ORIGIN=http://localhost:5173
# MODEL_PATH=/absolute/path/to/primary_best.pth
# SUPABASE_STORAGE_BUCKET=prediction-images  -- must exist in your Supabase project's Storage
