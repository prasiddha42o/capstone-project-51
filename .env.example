# Read by docker-compose.yml to build the frontend image (Vite bakes these
# in at build time). Backend secrets live in backend/.env instead -- see
# backend/.env.example.
VITE_API_BASE_URL=http://localhost:8000
VITE_SUPABASE_URL=https://ccgafjswljywlpjvuonj.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_ANON_KEY
